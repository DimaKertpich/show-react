import { Stack } from '@mui/material';
import { FC } from 'react';
import AboutUsPage from './AboutUsPage';

const AboutUsPageContainer: FC = () => {
  return (
    <Stack>
      <AboutUsPage />
    </Stack>
  );
};

export default AboutUsPageContainer;
